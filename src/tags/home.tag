<home>
    <toolbar></toolbar>

    <sample-output store={this.opts.store}>
    </sample-output>

    <hr/>

    <title-form store={this.opts.store}>
    </title-form>

    <script>
     var store = this.opts.store;
    </script>
</home>
